apiVersion: v1
kind: Service

metadata:
  name: backend
  namespace: devops-app

spec:
  selector:
    app: backend

  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}

  type: {{ .Values.service.type }}
