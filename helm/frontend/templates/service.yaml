apiVersion: v1
kind: Service

metadata:
  name: frontend
  namespace: devops-app

spec:
  selector:
    app: frontend

  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}

  type: {{ .Values.service.type }}
